import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { AppOrientationWrapper } from './AppOrientationWrapper';

ReactDOM.render(<AppOrientationWrapper />, document.getElementById("root"));
